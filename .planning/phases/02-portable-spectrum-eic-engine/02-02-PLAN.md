---
phase: 02-portable-spectrum-eic-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/io/eic.rs
  - src/io/infer_format/mod.rs
autonomous: true
requirements:
  - EIC-03
  - EIC-04
  - TEST-03
user_setup: []
must_haves:
  truths:
    - "A simple expected-result regression path exists for the shared EIC engine so obvious query or filtering mistakes are visible quickly."
    - "The portable tests exercise the same lazy shared engine that production callers use, not a format-specific shortcut or a large fixture-only harness."
    - "The regression coverage is small, deterministic, and specific enough to catch a fallback to eager or linearly-scanned behavior."
  artifacts:
    - path: "src/io/eic.rs"
      provides: "Focused unit-test coverage for the portable helpers and spectrum-processing semantics"
      contains: "#[cfg(test)]"
    - path: "src/io/infer_format/mod.rs"
      provides: "Shared regression harness for the public reader-facing EIC path"
      contains: "manual_extract"
    - path: "src/io/infer_format/mod.rs"
      provides: "A tiny expected-result check that makes the shared extraction output easy to compare against"
      contains: "extract_eic"
  key_links:
    - from: "src/io/eic.rs"
      to: "src/io/infer_format/mod.rs"
      via: "portable engine helpers and public-reader regression tests should agree on the same query semantics"
      pattern: "EICQuery|ExtractedIonChromatogram"
    - from: "src/io/infer_format/mod.rs"
      to: "src/io/eic.rs"
      via: "the test harness compares the shared extraction result against a simple expected result or manual reference"
      pattern: "manual_extract|extract_eic"
---

<objective>
Lock in a lightweight regression anchor for the portable EIC engine so the lazy spectrum path and ordered-array narrowing are easy to validate and hard to accidentally regress.

Purpose: Add a small, deterministic proof that the shared EIC logic still returns the right points and intensities without depending on a heavy or format-specific benchmark harness.
Output: Focused tests in `src/io/eic.rs` and/or `src/io/infer_format/mod.rs` that assert the shared portable path against a simple expected-result reference and, where useful, a tiny fixture-backed comparison.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-portable-spectrum-eic-engine/02-CONTEXT.md
@src/io/eic.rs
@src/io/infer_format/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a small expected-result regression anchor for the portable EIC engine</name>
  <files>src/io/eic.rs, src/io/infer_format/mod.rs</files>
  <action>Create a compact regression test that exercises the shared portable EIC path against a simple expected-result reference. Prefer a synthetic or in-memory setup first, so the test is deterministic and easy to reason about, and keep the point set small enough that the intended times and intensities are obvious. Verify the shared path preserves the expected chromatogram shape, including zero-intensity points for spectra that pass filtering.</action>
  <verify>Run the new regression test and confirm the exact expected times and intensities match the shared extraction result.</verify>
  <done>The shared EIC engine has a plain, deterministic regression anchor that catches obvious output mistakes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the portable path still behaves lazily and interval-aware under regression coverage</name>
  <files>src/io/eic.rs</files>
  <action>Extend the regression coverage just enough to show the portable engine still behaves like the intended lazy fallback: spectra are accessed on demand, ordered-array matches are narrowed before summation, and the fallback remains usable when ordered arrays are absent. Keep the test surface tiny and targeted; do not build a broad fixture harness or a new helper layer just for the test.</action>
  <verify>Confirm the test would fail if the shared path reverted to eager whole-file materialization or to a naive full-array scan for ordered spectra.</verify>
  <done>The regression coverage protects the lazy/binary-search contract with the smallest useful test footprint.</done>
</task>

<task type="auto">
  <name>Task 3: Tie the new regression anchor into the existing public-reader test suite</name>
  <files>src/io/infer_format/mod.rs</files>
  <action>If the shared portable check belongs more naturally beside the existing reader-facing regression helpers, wire it into the current `manual_extract`/`extract_eic` test neighborhood instead of duplicating similar logic elsewhere. Keep any fixture-backed check tiny, feature-gated when appropriate, and aligned with the existing public EIC API so maintainers can spot obvious regressions without running the full ignored desktop suite.</action>
  <verify>Run the narrow reader-facing test target and confirm the new anchor and any fixture-backed comparison pass under the relevant feature set.</verify>
  <done>The public-reader regression suite now contains a small, obvious expected-result path for the portable EIC engine.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] At least one deterministic expected-result test covers the shared portable EIC path
- [ ] The regression anchor is small enough to catch obvious failures without depending on a large benchmark harness
- [ ] The portable path still exercises lazy spectrum access and interval-aware summation in the test coverage
- [ ] Any fixture-backed check stays tiny and aligned with the existing reader-facing test helpers
- [ ] The new coverage maps cleanly to TEST-03 and does not reopen the public API surface from Phase 1
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The phase has a simple expected-result regression path for the shared EIC engine
- The regression path is small, deterministic, and useful for spotting obvious behavior drift
- The phase stays focused on portability and regression safety rather than backend-specific optimizations
</success_criteria>

<output>
After completion, create `.planning/phases/02-portable-spectrum-eic-engine/02-02-SUMMARY.md`
</output>
