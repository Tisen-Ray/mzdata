---
phase: 02-portable-spectrum-eic-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/io/eic.rs
  - src/io/infer_format/dispatch.rs
autonomous: true
requirements:
  - EIC-03
  - EIC-04
user_setup: []
must_haves:
  truths:
    - "The default shared EIC path stays lazy by reading spectra on demand through the reader-native API instead of materializing the whole file."
    - "When ordered m/z arrays are present, the shared summation path narrows each spectrum to the matching interval with partition-point style bounds before summing intensities."
    - "The portable fallback remains a disciplined refinement of the existing `extract_eics_from_spectra` path, not a new analytical abstraction layer."
  artifacts:
    - path: "src/io/eic.rs"
      provides: "Portable shared extraction loop and interval-sum helper that implement the lazy spectrum path"
      contains: "extract_eics_from_spectra"
    - path: "src/io/eic.rs"
      provides: "Array-range summation logic that uses ordered m/z bounds before accumulation"
      contains: "partition_point"
    - path: "src/io/infer_format/dispatch.rs"
      provides: "Reader-dispatch forwarding that keeps the portable engine behind the same public EIC API"
      contains: "ExtractedIonChromatogramSource"
  key_links:
    - from: "src/io/infer_format/dispatch.rs"
      to: "src/io/eic.rs"
      via: "MZReaderType continues to route unsupported or generic readers into the shared portable EIC implementation"
      pattern: "extract_eics_from_spectra"
    - from: "src/io/eic.rs"
      to: "src/io/eic.rs"
      via: "query preparation and per-spectrum processing remain local and additive"
      pattern: "PreparedEICQuery|process_spectrum|sum_array_range"
---

<objective>
Make the portable cross-format EIC engine the clean default behind `extract_eic` and `extract_eics`, keeping the implementation lazy, interval-aware, and close to the existing reader pipeline.

Purpose: Tighten the shared fallback so Phase 2 proves the portable strategy already selected in Phase 1 without introducing a second analytical architecture.
Output: A small refinement in `src/io/eic.rs` and the dispatch surface that preserves on-demand spectrum reads, uses ordered-array narrowing when available, and keeps the fallback easy to reason about.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-portable-spectrum-eic-engine/02-CONTEXT.md
@src/io/eic.rs
@src/io/infer_format/dispatch.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep the portable extraction loop explicitly lazy and local to src/io/eic.rs</name>
  <files>src/io/eic.rs</files>
  <action>Refine the shared `extract_eics_from_spectra` path so the portable behavior is obvious in one place: prepare queries once, switch to `DetailLevel::Lazy`, iterate spectra on demand, and keep the per-spectrum matching/summing helpers small enough that the lazy fallback reads like a disciplined refinement of the current code. Preserve the current spectrum-oriented contract, preserve zero-intensity points for spectra that pass the filters, and do not introduce new public analytical abstractions or a broader index layer.</action>
  <verify>Inspect `src/io/eic.rs` to confirm the portable fallback still uses on-demand `get_spectrum_by_index` access, keeps the lazy detail-level switch local, and leaves the public EIC API unchanged.</verify>
  <done>The shared fallback reads as the canonical lazy portable implementation and stays close to the existing reader flow.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow ordered-array summation before accumulation</name>
  <files>src/io/eic.rs</files>
  <action>Strengthen the existing ordered-array fast path so the interval narrowing is plainly spectrum-internal and portable: when a spectrum exposes ordered `m/z` and intensity arrays, locate the matching interval with partition-point style bounds before summing. Keep the fallback to peak iteration for readers that do not expose ordered arrays, and keep the optimization local to the shared helper instead of spreading format-specific logic.</action>
  <verify>Confirm the array path still sums only the matching interval, falls back cleanly when ordered arrays are missing, and does not change the public query contract or broaden the search window.</verify>
  <done>The shared engine does interval narrowing before summation wherever ordered arrays are available.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the public dispatch surface pointed at the shared fallback</name>
  <files>src/io/infer_format/dispatch.rs</files>
  <action>Review the `MZReaderType` EIC forwarding so the generic readers and dispatch wrapper continue to route through the same shared portable engine without introducing new branching or service-style indirection. Only adjust comments or forwarding code if needed to keep the implementation clearly additive and easy to extend in later backend-specific phases.</action>
  <verify>Inspect the dispatch implementation and confirm the shared EIC trait still hangs off the normal reader hierarchy with no new public analytical entry point.</verify>
  <done>The portable engine remains hidden behind the existing reader-native API and the dispatch layer stays lightweight.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/io/eic.rs` still performs on-demand spectrum access through the shared reader loop
- [ ] Ordered-array spectra are narrowed with partition-point style bounds before summation
- [ ] The fallback to peak iteration remains intact for spectra without ordered arrays
- [ ] `MZReaderType` dispatch still forwards the shared EIC surface without a new abstraction layer
- [ ] The phase still stays within the portable fallback boundary and does not pull in backend-specific acceleration work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The default shared EIC path is clearly lazy and portable
- Ordered `m/z` arrays are handled with binary-search style narrowing before intensity accumulation
- The phase keeps the smallest viable refinement of the current EIC path instead of expanding the public model
</success_criteria>

<output>
After completion, create `.planning/phases/02-portable-spectrum-eic-engine/02-01-SUMMARY.md`
</output>
