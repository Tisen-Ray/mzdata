---
phase: 01-public-eic-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/io/eic.rs
  - src/io/infer_format/dispatch.rs
  - src/io/infer_format/mod.rs
autonomous: true
requirements:
  - EIC-01
  - EIC-02
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Callers can request a single EIC or a batch of EICs through MZReader trait methods without choosing a backend-specific API."
    - "The public EIC query contract supports the full required Phase 1 filter set together: required mz range plus optional RT range, MS level, mobility range, and minimum intensity."
    - "The Phase 1 EIC surface remains additive inside the existing reader hierarchy instead of introducing a parallel service-style access path."
  artifacts:
    - path: "src/io/eic.rs"
      provides: "Public EIC query/result types and the ExtractedIonChromatogramSource trait with extract_eic and extract_eics entry points"
      min_lines: 120
      contains: "pub trait ExtractedIonChromatogramSource"
    - path: "src/io/infer_format/dispatch.rs"
      provides: "MZReader dispatch forwarding for the shared EIC trait surface"
      contains: "ExtractedIonChromatogramSource"
    - path: "src/io/infer_format/mod.rs"
      provides: "Focused regression tests for dispatch and the complete public EIC query contract, including combined optional filters"
      contains: "extract_eic"
  key_links:
    - from: "src/io/infer_format/dispatch.rs"
      to: "src/io/eic.rs"
      via: "MZReaderType implements the shared EIC trait and forwards extract_eic/extract_eics"
      pattern: "ExtractedIonChromatogramSource"
    - from: "src/io/infer_format/mod.rs"
      to: "src/io/infer_format/dispatch.rs"
      via: "targeted tests exercise the public MZReader EIC surface"
      pattern: "extract_eic"
    - from: "src/io/infer_format/mod.rs"
      to: "src/io/eic.rs"
      via: "API-facing tests build queries using mz bounds plus RT, MS level, mobility, and minimum intensity filters"
      pattern: "with_rt_range|with_ms_level|with_mobility_range|with_min_intensity"
---

<objective>
Finalize the core public EIC contract around the reader trait entry points that Phase 1 has already selected.

Purpose: Lock the public API surface before deeper extraction work begins, so later phases build on a stable reader-native contract instead of revisiting placement or naming.
Output: A tightened `src/io/eic.rs` public contract, dispatch wiring that clearly exposes `extract_eic` / `extract_eics` through `MZReader`, and focused regression coverage for the public surface.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-eic-surface/01-CONTEXT.md
@src/io/eic.rs
@src/io/infer_format/dispatch.rs
@src/io/infer_format/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the public EIC query and result contract in src/io/eic.rs</name>
  <files>src/io/eic.rs</files>
  <action>Review `EICQuery`, `ExtractedIonChromatogram`, `EICError`, and `ExtractedIonChromatogramSource` as the Phase 1 contract surface. Add or refine doc comments, constructor/builder naming polish, and any tiny helper extraction needed to make the selected scope explicit: mz range required, RT/MS level/mobility/minimum intensity optional, dedicated extracted result type preserved. Do not expand into TIC, DT, tolerance modes, polarity, precursor-window filtering, or a deeper module tree.</action>
  <verify>Inspect `src/io/eic.rs` to confirm the public types and trait docs describe the locked Phase 1 surface and that no new out-of-scope filters or alternate entry points were introduced.</verify>
  <done>`src/io/eic.rs` reads as the canonical Phase 1 API contract and keeps the result semantics distinct from file-native chromatograms.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the reader-native entry points explicit in MZReader dispatch</name>
  <files>src/io/eic.rs, src/io/infer_format/dispatch.rs</files>
  <action>Ensure the trait-based calling style `reader.extract_eic(...)` and `reader.extract_eics(...)` remains the obvious public path across `MZReaderType` dispatch. Clean up any naming, forwarding, or nearby comments needed so the shared trait sits naturally beside existing reader APIs. Preserve the standalone `src/io/eic.rs` module and additive dispatch approach; do not introduce a helper service object or broader analytical abstraction in this phase.</action>
  <verify>Inspect `src/io/infer_format/dispatch.rs` and confirm `MZReaderType` still implements and forwards the shared EIC trait without format-specific branching leaking into the public API.</verify>
  <done>The public EIC entry point is clearly the reader trait method surface and stays aligned with the existing `MZReader` hierarchy.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests that pin the public surface without drifting into Phase 2</name>
  <files>src/io/infer_format/mod.rs, src/io/eic.rs</files>
  <action>Add or refine lightweight API-facing tests that prove the public dispatch shape and the complete EIC-02 contract, not just individual builder methods. Include at least one focused path that constructs a query with required mz bounds plus optional RT, MS level, mobility, and minimum intensity filters together, and verifies that the resulting query/validation surface preserves those fields correctly. Keep the tests small and public-surface oriented; do not turn this plan into spectrum-engine regression work or real-data validation.</action>
  <verify>Run targeted Rust tests covering the EIC dispatch/query contract, including the combined-filter query case, such as `cargo test extract_eic --lib` or a similarly scoped command that exercises the affected public API tests.</verify>
  <done>There is a compact regression anchor proving both the reader-trait EIC surface and the full Phase 1 public query contract, including all required optional filters together.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test extract_eic --lib` or equivalent targeted public-surface test command passes
- [ ] At least one targeted test proves a query can carry mz bounds plus RT, MS level, mobility, and minimum intensity filters together through the public API surface
- [ ] Public EIC trait docs and names in `src/io/eic.rs` match the locked Phase 1 decisions
- [ ] `MZReaderType` dispatch still exposes the shared EIC trait without introducing a new service-style API
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `extract_eic` and `extract_eics` are the clear public reader entry points for cross-format callers
- The query contract remains intentionally small and matches EIC-02 with executable proof for mz range plus optional RT, MS level, mobility, and minimum intensity
- The EIC surface fits naturally into the existing reader/module architecture
</success_criteria>

<output>
After completion, create `.planning/phases/01-public-eic-surface/01-01-SUMMARY.md`
</output>
