---
phase: 01-public-eic-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/io/mod.rs
  - src/lib.rs
  - src/prelude.rs
autonomous: true
requirements:
  - EIC-01
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "The finalized Phase 1 EIC API is discoverable from the normal io, crate-root, and prelude public surfaces."
    - "Public naming and lightweight docs make it clear that extracted EICs are computed views exposed through the reader APIs, not native chromatogram objects."
    - "The export layout stays flat and additive, leaving src/io/eic.rs as the focused home for this feature."
  artifacts:
    - path: "src/io/mod.rs"
      provides: "Primary io-module re-exports for the public EIC surface"
      contains: "ExtractedIonChromatogram"
    - path: "src/lib.rs"
      provides: "Crate-root re-exports and crate-level API discoverability for EIC entry types"
      contains: "ExtractedIonChromatogramSource"
    - path: "src/prelude.rs"
      provides: "Prelude exposure for callers using trait-based reader workflows"
      contains: "ExtractedIonChromatogramSource"
  key_links:
    - from: "src/lib.rs"
      to: "src/io/mod.rs"
      via: "crate root re-exports the finalized io-level EIC types"
      pattern: "pub use crate::io::"
    - from: "src/prelude.rs"
      to: "src/io/eic.rs"
      via: "prelude exposes the shared EIC trait and core types for reader-centric consumers"
      pattern: "ExtractedIonChromatogramSource"
---

<objective>
Make the finalized Phase 1 EIC surface coherent and easy to discover from the repository's normal public export paths.

Purpose: Ensure the new public API feels native to `mzdata` callers, with flat additive placement and just enough documentation polish to avoid confusion before deeper implementation phases begin.
Output: Clean re-export wiring in `src/io/mod.rs`, `src/lib.rs`, and `src/prelude.rs`, plus only the minimal export-adjacent doc polish needed to explain where callers find the public EIC surface.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-eic-surface/01-CONTEXT.md
@.planning/phases/01-public-eic-surface/01-01-SUMMARY.md
@src/io/mod.rs
@src/lib.rs
@src/prelude.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the additive re-export path for the public EIC API</name>
  <files>src/io/mod.rs, src/lib.rs, src/prelude.rs</files>
  <action>Review and refine the public re-export surfaces so the Phase 1 EIC types and trait are available where `mzdata` users naturally look: `io`, crate root, and prelude. Preserve the existing flat module strategy with `src/io/eic.rs` as the focused implementation file. Keep the changes additive and avoid restructuring modules or introducing special-case export layers that would make later analytical work harder to extend.</action>
  <verify>Inspect `src/io/mod.rs`, `src/lib.rs`, and `src/prelude.rs` to confirm the same finalized EIC types/trait are exposed consistently and without accidental omissions.</verify>
  <done>The EIC surface is reachable through the standard public entry points and matches the repository's export conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Add export-adjacent doc cues that point callers to the finalized EIC surface</name>
  <files>src/io/mod.rs, src/lib.rs, src/prelude.rs</files>
  <action>Add only the minimal doc comments or re-export-adjacent wording needed so a caller scanning `io`, crate root, or prelude can tell that EIC extraction lives on reader trait methods and is exported through these standard surfaces. Keep this scoped to the touched export files only; do not reopen `src/io/eic.rs`, restate the core query contract, or pull in README/tutorial work reserved for Phase 4.</action>
  <verify>Review the export files and confirm the added wording improves discoverability without changing API ownership or duplicating the detailed contract already finalized in Plan 01.</verify>
  <done>A caller scanning the export surfaces can see where the EIC API lives and which re-exports to use, without additional contract work in implementation files.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the export surface is complete and internally consistent</name>
  <files>src/io/mod.rs, src/lib.rs, src/prelude.rs</files>
  <action>Perform a specific consistency check after the re-export and doc-cue changes: ensure the same finalized EIC trait and types are exposed from all three public surfaces, with no missing symbol, stale ordering issue, or conflicting wording. Limit code changes in this task to any direct fixes required in those export files.</action>
  <verify>Run a narrow compile/test command covering the touched export surface, such as `cargo test extract_eic --lib`, or `cargo test --lib` only if the narrower command is insufficient after re-export changes.</verify>
  <done>The finalized Phase 1 export surface is complete, consistent across `io`/crate root/prelude, and free of export-level mismatches.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Re-export surfaces in `src/io/mod.rs`, `src/lib.rs`, and `src/prelude.rs` are consistent
- [ ] Any added docs are limited to export-adjacent discoverability cues in those files
- [ ] Targeted compile/test verification passes after the export and polish pass
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The public EIC API is easy to find from the normal mzdata export surfaces
- Export placement remains flat and additive, with `src/io/eic.rs` as the focused home
- Wave 2 work stays confined to exports/discoverability and does not reopen the core EIC contract owned by Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/01-public-eic-surface/01-02-SUMMARY.md`
</output>
