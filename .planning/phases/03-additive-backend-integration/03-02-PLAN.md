---
phase: 03-additive-backend-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/io/infer_format/mod.rs
  - src/io/tdf/reader.rs
autonomous: true
requirements:
  - EIC-05
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "Backend-specific acceleration is proven through the same public reader API, not through internal-only tests or a separate public entry point."
    - "Existing spectrum and frame access behavior remains stable after the EIC integration work, so Phase 3 does not trade compatibility for optimization."
    - "There is focused regression coverage for the TDF fast-path/fallback boundary so silent divergence from portable semantics is visible quickly."
  artifacts:
    - path: "src/io/infer_format/mod.rs"
      provides: "Reader-facing regression tests for TDF EIC behavior and compatibility expectations"
      contains: "test_extract_eic_dispatch_tdf"
    - path: "src/io/tdf/reader.rs"
      provides: "Any backend-local helpers needed to expose or trigger fallback behavior under test"
      contains: "extract_eics_fast"
  key_links:
    - from: "src/io/infer_format/mod.rs"
      to: "src/io/tdf/reader.rs"
      via: "public-reader tests should prove the backend-local optimization and fallback preserve the same result contract"
      pattern: "extract_eic|extract_eics"
    - from: "src/io/infer_format/mod.rs"
      to: "src/io/eic.rs"
      via: "manual or portable reference extraction remains the semantic oracle for compatibility checks"
      pattern: "manual_extract"
---

<objective>
Add the regression and compatibility proof that Phase 3 needs: the TDF optimization still behaves through the same public EIC API, existing spectrum/frame operations remain stable, and the fast-path/fallback boundary stays visible to maintainers.

Purpose: Close the phase by proving the additive backend integration is correct and compatible, rather than only plausible from the implementation shape.
Output: Focused reader-facing tests and any tiny backend-local test hooks needed to compare optimized extraction against manual or portable behavior while protecting existing reader APIs.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-additive-backend-integration/03-CONTEXT.md
@src/io/eic.rs
@src/io/tdf/reader.rs
@src/io/infer_format/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a focused regression check for the TDF fast-path versus fallback boundary</name>
  <files>src/io/infer_format/mod.rs, src/io/tdf/reader.rs</files>
  <action>Extend the existing TDF reader-facing EIC tests so at least one targeted check exercises a query shape that should stay on the optimized path and one that should fall back to the portable path, then compare both against the same manual or portable reference. Keep the harness lightweight and feature-gated; avoid introducing heavy fixtures or broad benchmarking as part of this phase.</action>
  <verify>Run the targeted TDF EIC regression tests and confirm both optimized and fallback cases preserve point counts, times, and intensities against the reference extraction path.</verify>
  <done>The TDF fast-path/fallback boundary is visible in focused regression coverage instead of being implicit in the implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Protect non-EIC spectrum and frame behavior from regression</name>
  <files>src/io/infer_format/mod.rs</files>
  <action>Add or refine compatibility checks around the existing TDF reader behavior that should remain unchanged by the Phase 3 work: spectrum access, iteration, detail-level handling, and frame-oriented operations. Reuse the current reader-facing test neighborhood where possible so the compatibility proof sits next to the EIC integration coverage rather than in an isolated test harness.</action>
  <verify>Confirm the compatibility tests still pass after the EIC routing changes and would catch an obvious regression in the existing TDF spectrum/frame APIs.</verify>
  <done>Phase 3 explicitly proves `ARCH-02` instead of assuming compatibility from code review alone.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the ignored local desktop regressions aligned with the new integration seam</name>
  <files>src/io/infer_format/mod.rs</files>
  <action>Review the existing ignored desktop Bruker tests and adjust them only as needed so they still make sense after the new fast-path/fallback routing is introduced. Preserve their role as optional machine-local confidence checks, and keep the default automated suite lightweight and deterministic.</action>
  <verify>Inspect the ignored desktop tests and confirm they still validate the shared public API against manual extraction without depending on any new public hook or backend-specific toggle.</verify>
  <done>The optional real-dataset regressions remain aligned with the additive Phase 3 design and useful for local confidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] At least one reader-facing regression covers the optimized TDF path through the public EIC API
- [ ] At least one reader-facing regression covers the fallback path through the same public EIC API
- [ ] Existing spectrum or frame behavior is explicitly checked for compatibility after the integration change
- [ ] Ignored local Bruker regressions still align with the shared public API and manual reference behavior
- [ ] The test footprint stays lightweight and additive instead of expanding into a heavy fixture/benchmark harness
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Reader-facing regression coverage proves the TDF integration preserves shared EIC semantics
- Compatibility coverage protects existing spectrum and frame behavior from regression
- The phase closes with focused, maintainable tests rather than a one-off backend-specific harness
</success_criteria>

<output>
After completion, create `.planning/phases/03-additive-backend-integration/03-02-SUMMARY.md`
</output>
