---
phase: 03-additive-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/io/eic.rs
  - src/io/tdf/reader.rs
  - src/io/infer_format/dispatch.rs
autonomous: true
requirements:
  - EIC-05
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - "The shared public EIC surface stays fixed at `extract_eic` and `extract_eics`, while backend-specific acceleration remains an internal implementation detail."
    - "The Bruker TDF path can take a native fast route for supported queries but falls back to the portable spectrum-based engine when the optimized path cannot preserve the shared semantics confidently."
    - "The internal structure leaves a narrow, additive seam for future TIC/DT work instead of scattering EIC-specific routing across unrelated modules."
  artifacts:
    - path: "src/io/tdf/reader.rs"
      provides: "Backend-local routing between the TDF fast path and the portable fallback"
      contains: "extract_eics"
    - path: "src/io/eic.rs"
      provides: "Portable shared extraction helpers reusable as the semantic baseline"
      contains: "extract_eics_from_spectra"
    - path: "src/io/infer_format/dispatch.rs"
      provides: "Public dispatch forwarding that keeps callers on the shared EIC API"
      contains: "ExtractedIonChromatogramSource"
  key_links:
    - from: "src/io/tdf/reader.rs"
      to: "src/io/eic.rs"
      via: "the TDF reader should be able to reuse the portable extraction path as a fallback without exposing a second public API"
      pattern: "extract_eics|extract_eics_from_spectra"
    - from: "src/io/infer_format/dispatch.rs"
      to: "src/io/tdf/reader.rs"
      via: "dispatch continues to call the same shared trait while the backend chooses optimization internally"
      pattern: "reader.extract_eics"
---

<objective>
Clarify and tighten the backend-specific execution seam for EIC extraction so the Bruker TDF optimization remains available behind the same public API, with the portable engine preserved as the semantic fallback.

Purpose: Deliver the core additive integration for Phase 3 without redesigning the reader model or turning backend acceleration into a public mode switch.
Output: A small set of internal routing changes in the TDF reader and shared EIC helpers that preserve public behavior, keep the fast path backend-local, and make fallback conditions explicit and easy to extend.
</objective>

<execution_context>
@C:/Users/ray/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ray/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-additive-backend-integration/03-CONTEXT.md
@src/io/eic.rs
@src/io/tdf/reader.rs
@src/io/infer_format/dispatch.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the TDF EIC path choose optimization internally instead of unconditionally forcing the fast path</name>
  <files>src/io/tdf/reader.rs, src/io/eic.rs</files>
  <action>Refine the TDF `ExtractedIonChromatogramSource` implementation so it decides at execution time whether a query batch can safely use the backend-native fast path or should reuse the portable shared engine. Keep that decision internal to the TDF module, preserve the current public `extract_eic` and `extract_eics` surface, and make the fallback explicit enough that maintainers can extend the same seam for later analytical work.</action>
  <verify>Inspect the TDF reader implementation and confirm unsupported or semantically risky query shapes can route into the portable extraction helpers without adding any new public API or format selector.</verify>
  <done>The TDF backend owns an internal fast-path-or-fallback decision while callers stay on the same public EIC interface.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the shared portable engine reusable as the semantic baseline</name>
  <files>src/io/eic.rs</files>
  <action>If the TDF fallback needs a slightly narrower helper seam, extract or expose only the minimal shared helper needed so backend readers can reuse the portable logic without duplicating query preparation or changing result semantics. Keep the helper EIC-specific and additive; do not broaden this into a generic analytical execution framework or move unrelated reader code.</action>
  <verify>Confirm the portable helper still preserves phase-1 and phase-2 semantics for RT, MS level, mobility, minimum intensity, and zero-intensity points while remaining usable by the backend-local fallback.</verify>
  <done>The shared portable engine remains the single semantic baseline and can be reused by optimized backends as a fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve lightweight dispatch and extension seams</name>
  <files>src/io/infer_format/dispatch.rs, src/io/tdf/reader.rs</files>
  <action>Review the infer-format dispatch and any nearby comments so the public reader hierarchy still reads as one API with backend-local optimizations behind it. Only make the smallest documentation or forwarding adjustments needed to keep the future TIC/DT seam understandable and to avoid scattering EIC routing logic across the dispatch layer.</action>
  <verify>Inspect the dispatch implementation and confirm the additive seam is still clear: dispatch forwards to the shared trait, and backend routing details stay localized to the backend implementation.</verify>
  <done>The file and trait layout still point future analytical work toward a narrow additive seam instead of a public redesign.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The public EIC entry points remain `extract_eic` and `extract_eics` only
- [ ] The TDF backend can fall back to the portable path for unsupported query shapes
- [ ] The portable fallback remains reusable without duplicating query/result semantics
- [ ] No new public backend selector, fast-path knob, or second analytical API was introduced
- [ ] The updated layout still reads as an additive extension seam for future TIC/DT work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend-specific acceleration remains hidden behind the existing public EIC surface
- The TDF integration has an explicit, narrow internal seam for optimization and fallback
- The code layout stays additive and ready for future analytical extensions without redesign
</success_criteria>

<output>
After completion, create `.planning/phases/03-additive-backend-integration/03-01-SUMMARY.md`
</output>
